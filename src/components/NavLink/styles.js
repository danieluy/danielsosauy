import makeStyles from '@material-ui/core/styles/makeStyles';

export default makeStyles(theme => {
  return {
    a: {
      textDecoration: 'none',
      color: 'inherit',
      outline: 'none',
    },
  };
});
